import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
  main: {
    // justifyContent: "left",
    marginTop: 10,
    marginLeft: 10,
    marginBottom: 10,
  },
  h2: {
    marginBottom: 15,
  },
  formGroup: {
    marginTop: 10,
    marginBottom: 10,
  },
  formElement: {
    marginTop: 10,
    marginBottom: 10,
  },
  button: {
    marginTop: 15,
    marginBottom: 15,
  },
  card: {
    padding: 15,
  },
}));

export default useStyles;
